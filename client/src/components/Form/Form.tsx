import React from 'react';

type Props = {
  obj: {};
  setObj: () => void;
  onSubmit: () => void;
};

const Form = ({ obj, setObj, onSubmit }: Props) => {
  return <form></form>;
};

export default Form;
